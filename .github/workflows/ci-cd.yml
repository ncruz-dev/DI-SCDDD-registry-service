name: Java CI with Maven

on:
  pull_request:
    branches: [ "main" ]
    types: [closed]

concurrency:
  group: docker-build
  cancel-in-progress: true

jobs:
  build:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # 👇 Creamos la variable corta de GithubSha para usarla en el tag de la imagen y evitar que sea demasiado larga
    - name: Set short SHA
      run: echo "SHORT_SHA=${GITHUB_SHA::7}" >> $GITHUB_ENV

    # 👇 Mejora performance + cacheo de dependencias
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

      # 👇 LOGIN OBLIGATORIO PARA PODER HACER PUSH A DOCKER HUB
    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

      # 👇 BUILD & PUSH DE LA IMAGEN A DOCKER HUB
    - name: Build & push Docker Image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: |
          docker.io/${{ secrets.DOCKER_USERNAME }}/eureka-server:${{ github.run_number }}-${{ env.SHORT_SHA }}
#          docker.io/${{ secrets.DOCKER_USERNAME }}/eureka-server:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max